server:
  port: 8080
encrypt:
  key: foobar
spring:
  application:
    name: microservice-config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/olay/config.git



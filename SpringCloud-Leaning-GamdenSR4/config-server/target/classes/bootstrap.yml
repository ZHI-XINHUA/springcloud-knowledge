server:
  port: 8080
spring:
  application:
    name: microservice-config-server
  cloud:
    config:
      server:
        git:
          # 配置git仓库地址
          uri: https://gitee.com/olay/config.git
          # git仓库的账号
          #username:
          # git仓库密码
          #password:
encrypt:
  key-store:
    location: server.jks
    password: zhixinhua
    alias: mytestkey
    secret: changeme